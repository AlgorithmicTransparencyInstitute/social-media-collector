/* eslint-disable react/prop-types */
import React from 'react';

const PreferenceSelector = ({ storageKey }) => <div id={storageKey} name="preference-selector" />;

export default PreferenceSelector;
